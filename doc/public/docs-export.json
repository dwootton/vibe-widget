{
  "generated_at": "2026-09-16T15:35:50.087Z",
  "site": {
    "name": "Vibe Widget",
    "description": "Notebook widgets generated from a prompt and your data.",
    "base_url": "https://vibewidget.dev"
  },
  "pages": [
    {
      "id": "installation",
      "path": "/docs",
      "title": "Installation",
      "description": "Install the package, set one API key, and create a first widget.",
      "content_html": "<p>Vibe Widget runs on Python 3.9 or newer, and it needs one API key to generate a widget.\nWidgets that were generated earlier and cached on disk open without a key.</p>\n<pre><code>pip install vibe-widget</code></pre>\n<h2>1. Add a key</h2>\n<p>Create a <code>.env</code> file next to your notebook holding any one of these lines:</p>\n<pre><code class=\"language-bash\">ANTHROPIC_API_KEY=sk-ant-...\nOPENAI_API_KEY=sk-...\nOPENROUTER_API_KEY=sk-or-...\n</code></pre>\n<p>Vibe Widget reads the <code>.env</code> file, picks the provider that matches the variable name, and uses that\nprovider&#x27;s default model. Nothing else needs to be set. Exporting the same variable in your shell\nworks the same way, and a value already in the environment is never overwritten by the file.</p>\n<p>You can get a key from <a href=\"https://console.anthropic.com/\">Anthropic</a>,\n<a href=\"https://platform.openai.com/\">OpenAI</a> or <a href=\"https://openrouter.ai/\">OpenRouter</a>. One OpenRouter key\nreaches models from every vendor.</p>\n<h2>2. Create a widget</h2>\n<div class=\"placeholder\"><div class=\"placeholder-label\">Widget Preview</div><div class=\"placeholder-caption\">Open the live docs to see this interactive widget.</div></div>\n<pre><code class=\"language-python\">import pandas as pd\nimport vibe_widget as vw\n\ndf = pd.read_csv(&quot;sales.csv&quot;)\nwidget = vw.create(&quot;bar chart of revenue by region&quot;, df)\nwidget\n</code></pre>\n<p>To see what was picked up, call <code>vw.config()</code> with no arguments:</p>\n<pre><code class=\"language-python\">vw.config()\n# Config(provider=&#x27;anthropic&#x27;, host=&#x27;api.anthropic.com&#x27;, model=&#x27;claude-opus-5&#x27;,\n#        key_source=&#x27;ANTHROPIC_API_KEY (/work/analysis/.env)&#x27;, environment=&#x27;vscode-like&#x27;, ...)\n</code></pre>\n<p>The output names the variable and file the key came from, and never prints the key itself.</p>\n<h2>Where it runs</h2>\n<p>The same two steps work in every supported host.</p>\n<ul>\n<li><strong>JupyterLab and Jupyter Notebook</strong> need nothing extra.</li>\n<li><strong>VS Code notebooks</strong> get React bundled into each widget, because VS Code has no import maps.</li>\n<li><strong>Positron</strong> is handled the same way as VS Code, and is detected through <code>POSITRON</code> rather than <code>VSCODE_PID</code>.</li>\n<li><strong>Google Colab</strong> generates code synchronously, because background threads break widget updates there.</li>\n<li><strong>Quarto</strong> works in <code>.qmd</code> documents rendered with the Jupyter engine. Put the <code>.env</code> file in the project directory.</li>\n</ul>\n<h2>Opening a widget someone else generated</h2>\n<p>Generated code is written to <code>.vibewidget/</code> in your project, and the folder can be committed to git.\nA collaborator who clones the repository can open those widgets with no API key at all, because\nloading cached code makes no model call. A key is needed again only when someone generates, edits,\naudits, or repairs a widget.</p>\n<h2>Brush selection example</h2>\n<div class=\"placeholder\"><div class=\"placeholder-label\">Widget Preview</div><div class=\"placeholder-caption\">Open the live docs to see this interactive widget.</div></div>\n<pre><code class=\"language-python\">import pandas as pd\nimport vibe_widget as vw\n\ndf = pd.read_csv(&quot;sales.csv&quot;)\n\nwidget = vw.create(\n    &quot;scatter plot with brush selection, and a linked histogram&quot;,\n    df,\n    outputs=vw.outputs(selected_indices=&quot;indices of selected points&quot;)\n)\n\nwidget\n</code></pre>\n<p>For help setting API keys inside Jupyter, see\n<a href=\"https://docs.google.com/document/d/e/2PACX-1vST8sEHdo90NsTdTFNjLi27YFT-81u2WQa7--qr0u4yk2aByE6Q5WIj-p8JYueEING5-fNdFNu2Aa3t/pub\">this guide</a>.</p>",
      "content_text": "Vibe Widget runs on Python 3.9 or newer, and it needs one API key to generate a widget. Widgets that were generated earlier and cached on disk open without a key. pip install vibe-widget 1. Add a key Create a .env file next to your notebook holding any one of these lines: ANTHROPIC_API_KEY=sk-ant-... OPENAI_API_KEY=sk-... OPENROUTER_API_KEY=sk-or-... Vibe Widget reads the .env file, picks the provider that matches the variable name, and uses that provider&#x27;s default model. Nothing else needs to be set. Exporting the same variable in your shell works the same way, and a value already in the environment is never overwritten by the file. You can get a key from Anthropic, OpenAI or OpenRouter. One OpenRouter key reaches models from every vendor. 2. Create a widget Widget PreviewOpen the live docs to see this interactive widget. import pandas as pd import vibe_widget as vw df = pd.read_csv(&quot;sales.csv&quot;) widget = vw.create(&quot;bar chart of revenue by region&quot;, df) widget To see what was picked up, call vw.config() with no arguments: vw.config() # Config(provider=&#x27;anthropic&#x27;, host=&#x27;api.anthropic.com&#x27;, model=&#x27;claude-opus-5&#x27;, # key_source=&#x27;ANTHROPIC_API_KEY (/work/analysis/.env)&#x27;, environment=&#x27;vscode-like&#x27;, ...) The output names the variable and file the key came from, and never prints the key itself. Where it runs The same two steps work in every supported host. JupyterLab and Jupyter Notebook need nothing extra. VS Code notebooks get React bundled into each widget, because VS Code has no import maps. Positron is handled the same way as VS Code, and is detected through POSITRON rather than VSCODE_PID. Google Colab generates code synchronously, because background threads break widget updates there. Quarto works in .qmd documents rendered with the Jupyter engine. Put the .env file in the project directory. Opening a widget someone else generated Generated code is written to .vibewidget/ in your project, and the folder can be committed to git. A collaborator who clones the repository can open those widgets with no API key at all, because loading cached code makes no model call. A key is needed again only when someone generates, edits, audits, or repairs a widget. Brush selection example Widget PreviewOpen the live docs to see this interactive widget. import pandas as pd import vibe_widget as vw df = pd.read_csv(&quot;sales.csv&quot;) widget = vw.create( &quot;scatter plot with brush selection, and a linked histogram&quot;, df, outputs=vw.outputs(selected_indices=&quot;indices of selected points&quot;) ) widget For help setting API keys inside Jupyter, see this guide."
    },
    {
      "id": "config",
      "path": "/docs/config",
      "title": "Configuration",
      "description": "Set the model, the endpoint, and the safety limits.",
      "content_html": "<p><code>vw.config()</code> reads and changes the settings every widget uses. Called with no arguments it reports\nwhat is currently in effect, and called with keyword arguments it changes those settings for the\nrest of the session.</p>\n<pre><code class=\"language-python\">import vibe_widget as vw\n\nvw.config(model=&quot;claude-sonnet-5&quot;)\nvw.config(mode=&quot;premium&quot;)\nvw.config(execution=&quot;approve&quot;)\n</code></pre>\n<h2>API key setup</h2>\n<p>Put one key in a <code>.env</code> file next to your notebook:</p>\n<pre><code class=\"language-bash\">ANTHROPIC_API_KEY=sk-ant-...\nOPENAI_API_KEY=sk-...\nOPENROUTER_API_KEY=sk-or-...\n</code></pre>\n<p>The file is looked for in the working directory and each parent directory, stopping at the\nrepository root or your home directory. A variable already present in the environment is never\noverwritten, so an exported value wins over the file. No extra package is needed, and you should not\ncall <code>dotenv.load_dotenv()</code> yourself.</p>\n<p>Exporting the variable works the same way:</p>\n<pre><code class=\"language-bash\">export ANTHROPIC_API_KEY=&#x27;your-key&#x27;\n</code></pre>\n<pre><code class=\"language-powershell\">$env:ANTHROPIC_API_KEY=&quot;your-key&quot;\n</code></pre>\n<p>Keep keys out of the notebook itself, because a hardcoded key is easy to commit or share by\naccident.</p>\n<h2>Provider inference</h2>\n<p>When you do not set <code>base_url</code>, the key you supplied decides both the endpoint and the default\nmodel.</p>\n<table><thead><tr><th>Variable</th><th>Endpoint</th><th>Default model</th></tr></thead><tbody><tr><td><code>VIBE_API_KEY</code></td><td><code>VIBE_BASE_URL</code>, or OpenRouter</td><td>depends on the endpoint</td></tr><tr><td><code>ANTHROPIC_API_KEY</code></td><td><code>https://api.anthropic.com/v1/</code></td><td><code>claude-opus-5</code></td></tr><tr><td><code>OPENAI_API_KEY</code></td><td><code>https://api.openai.com/v1</code></td><td><code>gpt-5.5</code></td></tr><tr><td><code>OPENROUTER_API_KEY</code></td><td><code>https://openrouter.ai/api/v1</code></td><td><code>google/gemini-3-flash-preview</code></td></tr></tbody></table>\n<p>The variables are checked in that order, so <code>VIBE_API_KEY</code> overrides everything and\n<code>OPENROUTER_API_KEY</code> is the last one tried. An explicit <code>vw.config(api_key=...)</code> beats all of them.\nWhen a key carries a prefix belonging to a different vendor, such as an <code>sk-ant-</code> value stored in\n<code>OPENAI_API_KEY</code>, the variable name decides the provider and a warning is printed once.</p>\n<p>Calling <code>vw.config()</code> with no arguments reports what was inferred, without printing the key:</p>\n<pre><code class=\"language-python\">vw.config()\n# Config(provider=&#x27;openai&#x27;, host=&#x27;api.openai.com&#x27;, model=&#x27;gpt-5.5&#x27;,\n#        key_source=&#x27;OPENAI_API_KEY (/work/analysis/.env)&#x27;, environment=&#x27;quarto&#x27;, ...)\n</code></pre>\n<p><code>provider</code> is derived from the endpoint and reads <code>anthropic</code>, <code>openai</code>, <code>openrouter</code> or <code>custom</code>.\n<code>environment</code> lists the notebook hosts detected for the running kernel, such as <code>vscode-like</code> for\nVS Code and Positron, <code>quarto</code>, <code>colab</code> or <code>pyodide</code>.</p>\n<p>Changing only the model keeps the provider your key selected:</p>\n<pre><code class=\"language-python\">vw.config(model=&quot;claude-sonnet-5&quot;)   # still Anthropic\n</code></pre>\n<p>A model id written in OpenRouter&#x27;s <code>vendor/name</code> form is routed to OpenRouter when\n<code>OPENROUTER_API_KEY</code> is set, even if an Anthropic or OpenAI key is also present, because that form\nof id exists only on OpenRouter. Going back to a plain model name returns you to the provider your\nother key selects.</p>\n<pre><code class=\"language-python\">vw.config(model=&quot;anthropic/claude-opus-4.5&quot;)   # OpenRouter, when its key is set\nvw.config(model=&quot;claude-sonnet-5&quot;)             # back to Anthropic\n</code></pre>\n<p>Without an OpenRouter key the same id raises a <code>ValueError</code> explaining that Anthropic&#x27;s own API does\nnot know the name, and telling you to use the plain name or point <code>base_url</code> at OpenRouter.</p>\n<h2>Endpoint</h2>\n<p>Set <code>base_url</code> to reach any other OpenAI-compatible server, including one running on your machine.</p>\n<pre><code class=\"language-python\">vw.config(base_url=&quot;http://localhost:11434/v1&quot;, model=&quot;qwen2.5-coder&quot;)   # Ollama\nvw.config(base_url=&quot;http://localhost:8000/v1&quot;, model=&quot;Qwen/Qwen2.5-Coder-32B&quot;)  # vLLM\n</code></pre>\n<p>An endpoint on <code>localhost</code>, <code>127.0.0.1</code> or <code>::1</code> needs no API key, because local servers ignore it.\nSet <code>VIBE_API_KEY</code> anyway when yours sits behind an auth proxy.</p>\n<p>For an endpoint with no dedicated variable, such as Azure OpenAI, set both <code>VIBE_API_KEY</code> and\n<code>VIBE_BASE_URL</code>:</p>\n<pre><code class=\"language-bash\">VIBE_API_KEY=your-key\nVIBE_BASE_URL=https://my-resource.openai.azure.com/openai/v1\n</code></pre>\n<p>Requests adapt to what the endpoint accepts. Newer OpenAI models want <code>max_completion_tokens</code>\ninstead of <code>max_tokens</code>, and some models reject <code>temperature</code> outright. The rejected parameter is\nrenamed or dropped and the correction is remembered, so only the first call to a given endpoint pays\nfor it.</p>\n<p><code>timeout</code> is the HTTP timeout in seconds for one model call, and it defaults to 120. Raise it for\nslow local models.</p>\n<pre><code class=\"language-python\">vw.config(timeout=300.0)\n</code></pre>\n<h2>Models and modes</h2>\n<p><code>vw.models</code> prints the catalog, and <code>vw.models.standard</code> and <code>vw.models.premium</code> return the default\nid for each tier.</p>\n<pre><code class=\"language-python\">vw.models\nvw.models.standard   # &quot;google/gemini-3-flash-preview&quot;\nvw.models.premium    # &quot;google/gemini-3-pro-preview&quot;\n</code></pre>\n<p><code>mode</code> picks between two OpenRouter defaults, a faster and cheaper one and a slower and stronger\none.</p>\n<pre><code class=\"language-python\">vw.config(mode=&quot;standard&quot;)   # google/gemini-3-flash-preview, the default\nvw.config(mode=&quot;premium&quot;)    # google/gemini-3-pro-preview\n</code></pre>\n<p>Setting <code>mode</code> swaps the model only while you are on one of those two defaults. Once you name a\nmodel yourself, your choice stays, and <code>mode</code> no longer changes it. The word <code>&quot;openrouter&quot;</code> can be\npassed as the model name to mean whichever id the current mode points to.</p>\n<pre><code class=\"language-python\">vw.config(mode=&quot;premium&quot;, model=&quot;openrouter&quot;)   # google/gemini-3-pro-preview\n</code></pre>\n<h2>Common options</h2>\n<pre><code class=\"language-python\">vw.config(\n    execution=&quot;auto&quot;,       # &quot;auto&quot; or &quot;approve&quot;\n    retry=2,                # automatic repair attempts per generation\n    agent_preset=&quot;project&quot;,\n    agent_run={&quot;permission_tier&quot;: 1, &quot;allowed_roots&quot;: [&quot;.&quot;]},\n    bypass_row_guard=False,\n    base_url=None,          # None means infer from whichever API key is set\n    timeout=120.0,          # HTTP timeout, seconds\n    data_privacy=&quot;sample&quot;,  # &quot;sample&quot; or &quot;schema&quot;\n    sample_rows=3,          # rows sent per input in &quot;sample&quot; mode\n)\n</code></pre>\n<p>The three settings that take fixed strings also have namespaces, so you can let autocomplete fill\nthem in: <code>vw.execution.auto</code>, <code>vw.execution.approve</code>, <code>vw.mode.standard</code>, <code>vw.mode.premium</code>,\n<code>vw.presets.safe</code>, <code>vw.presets.project</code> and <code>vw.presets.connected</code>.</p>\n<h2>Automatic repair</h2>\n<p>When generated code throws an error in the browser, the error is sent back to the model and the\nmodel is asked to fix it. <code>retry</code> is how many of those attempts one generation is allowed.</p>\n<pre><code class=\"language-python\">vw.config(retry=2)   # the default\nvw.config(retry=0)   # never repair automatically\n</code></pre>\n<p>The budget covers a single generation. A repair that succeeds does not refill it, so a widget that\nkeeps failing cannot loop indefinitely, and a new generation starts with a full budget again. Once\nthe attempts are used up the widget&#x27;s status becomes <code>blocked</code>, and a log line tells you to describe\nthe fix in the prompt box or to raise the budget with <code>vw.config(retry=N)</code>. Typing a fix into the\nprompt box still works at <code>retry=0</code>, because the budget only governs the automatic attempts.</p>\n<h2>Cost</h2>\n<p>Each widget counts the tokens it spent:</p>\n<pre><code class=\"language-python\">widget.usage\n# {&#x27;prompt_tokens&#x27;: 18432, &#x27;completion_tokens&#x27;: 5120, &#x27;requests&#x27;: 2}\n</code></pre>\n<p>Loading a cached widget makes no request, so the counts stay at zero until something calls the\nmodel. With no API key set there is no provider at all, and <code>widget.usage</code> is an empty dict.</p>\n<h2>Safety and permissions</h2>\n<p>Generated code runs in the notebook page with the same privileges as the page. It can reach the DOM,\ncookies and <code>localStorage</code> for the notebook origin, and any network endpoint the browser is allowed\nto call. There is no browser-level sandbox around it.</p>\n<h3>Execution mode</h3>\n<p><code>execution</code> decides whether generated code runs as soon as it arrives or waits for you.</p>\n<pre><code class=\"language-python\">vw.config(execution=&quot;approve&quot;)   # review the code before it runs\nvw.config(execution=&quot;auto&quot;)      # run immediately, the default\n</code></pre>\n<p>In <code>&quot;approve&quot;</code> mode the code does not execute until you approve it. Approval is decided in Python\nrather than in the browser, so a widget cannot approve itself. The widget stores a hash of the\ncode you approved, and if the code changes through an edit or a repair the approval resets and you\nare asked again.</p>\n<h3>Agent presets</h3>\n<p>An agent preset decides which tools the model is allowed to call while it works.</p>\n<table><thead><tr><th>Preset</th><th>Permission tier</th><th>File access</th><th>Network</th><th>Use case</th></tr></thead><tbody><tr><td><code>&quot;safe&quot;</code></td><td>0 (read-only)</td><td>Sandbox only</td><td>No</td><td>Sensitive data, minimal risk</td></tr><tr><td><code>&quot;project&quot;</code></td><td>1 (read-write)</td><td>Working dir + sandbox</td><td>No</td><td>General use (default)</td></tr><tr><td><code>&quot;connected&quot;</code></td><td>2 (full)</td><td>Working dir + sandbox</td><td>HTTPS only</td><td>Fetching remote data</td></tr></tbody></table>\n<pre><code class=\"language-python\">vw.config(agent_preset=&quot;safe&quot;)        # read-only tools, sandboxed\nvw.config(agent_preset=&quot;project&quot;)     # read-write in your project directory, the default\nvw.config(agent_preset=&quot;connected&quot;)   # also allowed to fetch HTTPS URLs\n</code></pre>\n<p><strong>Tier 0 tools</strong> are <code>data.profile</code>, <code>fs.list</code>, <code>fs.read</code>, <code>fs.exists</code>, <code>fs.glob</code>, <code>state.get</code>, <code>state.put</code> and <code>pls.describe</code>.</p>\n<p><strong>Tier 1 adds</strong> <code>data.load</code>, <code>fs.write</code>, <code>fs.mkdir</code> and <code>fs.read_base64</code>.</p>\n<p><strong>Tier 2 adds</strong> <code>net.fetch</code>, restricted to HTTPS with an allowlist for hosts and MIME types, and it requires <code>allow_net_fetch</code>.</p>\n<h3>Row guard</h3>\n<p>An input of more than 100,000 rows raises a <code>ValueError</code> naming the row count, so that a large\nframe cannot exhaust memory while it is being prepared for the widget.</p>\n<pre><code class=\"language-python\">vw.config(bypass_row_guard=False)   # keep the guard, the default\nvw.config(bypass_row_guard=True)    # allow larger datasets\n</code></pre>\n<h3>A locked-down configuration</h3>\n<p>For sensitive data or a shared machine, the settings below turn off everything that acts on its own:</p>\n<pre><code class=\"language-python\">vw.config(\n    execution=&quot;approve&quot;,          # require manual approval\n    agent_preset=&quot;safe&quot;,          # read-only tools, sandboxed\n    bypass_row_guard=False,       # enforce the row limit\n    retry=0,                      # no automatic code repairs\n)\n</code></pre>\n<h2>What is sent to the model provider</h2>\n<p>Vibe Widget sends the following:</p>\n<ul>\n<li>your prompt and theme prompt</li>\n<li>a schema summary of each input, covering shape, column names, dtypes, null counts, cardinality, and numeric min, max and mean</li>\n<li>the first <code>sample_rows</code> rows of each input, 3 by default, when <code>data_privacy</code> is <code>&quot;sample&quot;</code></li>\n<li>the declared inputs, outputs and actions</li>\n<li>the full widget code for edits, audits, and runtime repairs</li>\n<li>runtime error messages, when a repair is running</li>\n</ul>\n<h3>Controlling what leaves the notebook</h3>\n<p><code>data_privacy</code> chooses between the two summary modes.</p>\n<pre><code class=\"language-python\">vw.config(data_privacy=&quot;schema&quot;)   # schema only, no cell values\nvw.config(data_privacy=&quot;sample&quot;)   # schema plus head rows, the default\nvw.config(sample_rows=1)           # fewer rows in &quot;sample&quot; mode\n</code></pre>\n<p>In <code>&quot;schema&quot;</code> mode the summary carries no cell values at all, so there are no head rows, no category\nexamples and no sampled values. Only the shape and the per-column description are sent. Inputs that\nare not tables are summarized by type and length, and their truncated <code>repr</code> is included in\n<code>&quot;sample&quot;</code> mode only.</p>\n<p><code>data_privacy</code> governs the summary built from the values you pass to <code>vw.create</code>. It does not\nrestrict the agent&#x27;s <code>data.profile</code> tool, which reads sample values from any file it is allowed to\nopen. Combine <code>data_privacy=&quot;schema&quot;</code> with <code>agent_preset=&quot;safe&quot;</code> to keep the agent out of your\nproject directory as well.</p>\n<p>No API keys are written to disk. Generated widgets and audit reports are stored locally under\n<code>.vibewidget/</code>.</p>\n<h2>When a request fails</h2>\n<p>A failed model call raises a <code>ProviderError</code> carrying a short explanation instead of the raw SDK\nexception. Each error names its kind, which is one of <code>auth</code>, <code>quota</code>, <code>not_found</code>, <code>rate_limit</code>,\n<code>connection</code>, <code>timeout</code>, <code>context_length</code> or <code>other</code>. A <code>quota</code> error means the key itself is valid\nbut its spending limit is used up, which is worth separating from <code>auth</code>, because rotating a working\nkey does not help. For <code>quota</code> and <code>not_found</code> the provider&#x27;s own sentence is passed through, since\nit usually names the exact limit or model, and the raw response body is never included.</p>",
      "content_text": "vw.config() reads and changes the settings every widget uses. Called with no arguments it reports what is currently in effect, and called with keyword arguments it changes those settings for the rest of the session. import vibe_widget as vw vw.config(model=&quot;claude-sonnet-5&quot;) vw.config(mode=&quot;premium&quot;) vw.config(execution=&quot;approve&quot;) API key setup Put one key in a .env file next to your notebook: ANTHROPIC_API_KEY=sk-ant-... OPENAI_API_KEY=sk-... OPENROUTER_API_KEY=sk-or-... The file is looked for in the working directory and each parent directory, stopping at the repository root or your home directory. A variable already present in the environment is never overwritten, so an exported value wins over the file. No extra package is needed, and you should not call dotenv.load_dotenv() yourself. Exporting the variable works the same way: export ANTHROPIC_API_KEY=&#x27;your-key&#x27; $env:ANTHROPIC_API_KEY=&quot;your-key&quot; Keep keys out of the notebook itself, because a hardcoded key is easy to commit or share by accident. Provider inference When you do not set base_url, the key you supplied decides both the endpoint and the default model. VariableEndpointDefault modelVIBE_API_KEYVIBE_BASE_URL, or OpenRouterdepends on the endpointANTHROPIC_API_KEYhttps://api.anthropic.com/v1/claude-opus-5OPENAI_API_KEYhttps://api.openai.com/v1gpt-5.5OPENROUTER_API_KEYhttps://openrouter.ai/api/v1google/gemini-3-flash-preview The variables are checked in that order, so VIBE_API_KEY overrides everything and OPENROUTER_API_KEY is the last one tried. An explicit vw.config(api_key=...) beats all of them. When a key carries a prefix belonging to a different vendor, such as an sk-ant- value stored in OPENAI_API_KEY, the variable name decides the provider and a warning is printed once. Calling vw.config() with no arguments reports what was inferred, without printing the key: vw.config() # Config(provider=&#x27;openai&#x27;, host=&#x27;api.openai.com&#x27;, model=&#x27;gpt-5.5&#x27;, # key_source=&#x27;OPENAI_API_KEY (/work/analysis/.env)&#x27;, environment=&#x27;quarto&#x27;, ...) provider is derived from the endpoint and reads anthropic, openai, openrouter or custom. environment lists the notebook hosts detected for the running kernel, such as vscode-like for VS Code and Positron, quarto, colab or pyodide. Changing only the model keeps the provider your key selected: vw.config(model=&quot;claude-sonnet-5&quot;) # still Anthropic A model id written in OpenRouter&#x27;s vendor/name form is routed to OpenRouter when OPENROUTER_API_KEY is set, even if an Anthropic or OpenAI key is also present, because that form of id exists only on OpenRouter. Going back to a plain model name returns you to the provider your other key selects. vw.config(model=&quot;anthropic/claude-opus-4.5&quot;) # OpenRouter, when its key is set vw.config(model=&quot;claude-sonnet-5&quot;) # back to Anthropic Without an OpenRouter key the same id raises a ValueError explaining that Anthropic&#x27;s own API does not know the name, and telling you to use the plain name or point base_url at OpenRouter. Endpoint Set base_url to reach any other OpenAI-compatible server, including one running on your machine. vw.config(base_url=&quot;http://localhost:11434/v1&quot;, model=&quot;qwen2.5-coder&quot;) # Ollama vw.config(base_url=&quot;http://localhost:8000/v1&quot;, model=&quot;Qwen/Qwen2.5-Coder-32B&quot;) # vLLM An endpoint on localhost, 127.0.0.1 or ::1 needs no API key, because local servers ignore it. Set VIBE_API_KEY anyway when yours sits behind an auth proxy. For an endpoint with no dedicated variable, such as Azure OpenAI, set both VIBE_API_KEY and VIBE_BASE_URL: VIBE_API_KEY=your-key VIBE_BASE_URL=https://my-resource.openai.azure.com/openai/v1 Requests adapt to what the endpoint accepts. Newer OpenAI models want max_completion_tokens instead of max_tokens, and some models reject temperature outright. The rejected parameter is renamed or dropped and the correction is remembered, so only the first call to a given endpoint pays for it. timeout is the HTTP timeout in seconds for one model call, and it defaults to 120. Raise it for slow local models. vw.config(timeout=300.0) Models and modes vw.models prints the catalog, and vw.models.standard and vw.models.premium return the default id for each tier. vw.models vw.models.standard # &quot;google/gemini-3-flash-preview&quot; vw.models.premium # &quot;google/gemini-3-pro-preview&quot; mode picks between two OpenRouter defaults, a faster and cheaper one and a slower and stronger one. vw.config(mode=&quot;standard&quot;) # google/gemini-3-flash-preview, the default vw.config(mode=&quot;premium&quot;) # google/gemini-3-pro-preview Setting mode swaps the model only while you are on one of those two defaults. Once you name a model yourself, your choice stays, and mode no longer changes it. The word &quot;openrouter&quot; can be passed as the model name to mean whichever id the current mode points to. vw.config(mode=&quot;premium&quot;, model=&quot;openrouter&quot;) # google/gemini-3-pro-preview Common options vw.config( execution=&quot;auto&quot;, # &quot;auto&quot; or &quot;approve&quot; retry=2, # automatic repair attempts per generation agent_preset=&quot;project&quot;, agent_run={&quot;permission_tier&quot;: 1, &quot;allowed_roots&quot;: [&quot;.&quot;]}, bypass_row_guard=False, base_url=None, # None means infer from whichever API key is set timeout=120.0, # HTTP timeout, seconds data_privacy=&quot;sample&quot;, # &quot;sample&quot; or &quot;schema&quot; sample_rows=3, # rows sent per input in &quot;sample&quot; mode ) The three settings that take fixed strings also have namespaces, so you can let autocomplete fill them in: vw.execution.auto, vw.execution.approve, vw.mode.standard, vw.mode.premium, vw.presets.safe, vw.presets.project and vw.presets.connected. Automatic repair When generated code throws an error in the browser, the error is sent back to the model and the model is asked to fix it. retry is how many of those attempts one generation is allowed. vw.config(retry=2) # the default vw.config(retry=0) # never repair automatically The budget covers a single generation. A repair that succeeds does not refill it, so a widget that keeps failing cannot loop indefinitely, and a new generation starts with a full budget again. Once the attempts are used up the widget&#x27;s status becomes blocked, and a log line tells you to describe the fix in the prompt box or to raise the budget with vw.config(retry=N). Typing a fix into the prompt box still works at retry=0, because the budget only governs the automatic attempts. Cost Each widget counts the tokens it spent: widget.usage # {&#x27;prompt_tokens&#x27;: 18432, &#x27;completion_tokens&#x27;: 5120, &#x27;requests&#x27;: 2} Loading a cached widget makes no request, so the counts stay at zero until something calls the model. With no API key set there is no provider at all, and widget.usage is an empty dict. Safety and permissions Generated code runs in the notebook page with the same privileges as the page. It can reach the DOM, cookies and localStorage for the notebook origin, and any network endpoint the browser is allowed to call. There is no browser-level sandbox around it. Execution mode execution decides whether generated code runs as soon as it arrives or waits for you. vw.config(execution=&quot;approve&quot;) # review the code before it runs vw.config(execution=&quot;auto&quot;) # run immediately, the default In &quot;approve&quot; mode the code does not execute until you approve it. Approval is decided in Python rather than in the browser, so a widget cannot approve itself. The widget stores a hash of the code you approved, and if the code changes through an edit or a repair the approval resets and you are asked again. Agent presets An agent preset decides which tools the model is allowed to call while it works. PresetPermission tierFile accessNetworkUse case&quot;safe&quot;0 (read-only)Sandbox onlyNoSensitive data, minimal risk&quot;project&quot;1 (read-write)Working dir + sandboxNoGeneral use (default)&quot;connected&quot;2 (full)Working dir + sandboxHTTPS onlyFetching remote data vw.config(agent_preset=&quot;safe&quot;) # read-only tools, sandboxed vw.config(agent_preset=&quot;project&quot;) # read-write in your project directory, the default vw.config(agent_preset=&quot;connected&quot;) # also allowed to fetch HTTPS URLs Tier 0 tools are data.profile, fs.list, fs.read, fs.exists, fs.glob, state.get, state.put and pls.describe. Tier 1 adds data.load, fs.write, fs.mkdir and fs.read_base64. Tier 2 adds net.fetch, restricted to HTTPS with an allowlist for hosts and MIME types, and it requires allow_net_fetch. Row guard An input of more than 100,000 rows raises a ValueError naming the row count, so that a large frame cannot exhaust memory while it is being prepared for the widget. vw.config(bypass_row_guard=False) # keep the guard, the default vw.config(bypass_row_guard=True) # allow larger datasets A locked-down configuration For sensitive data or a shared machine, the settings below turn off everything that acts on its own: vw.config( execution=&quot;approve&quot;, # require manual approval agent_preset=&quot;safe&quot;, # read-only tools, sandboxed bypass_row_guard=False, # enforce the row limit retry=0, # no automatic code repairs ) What is sent to the model provider Vibe Widget sends the following: your prompt and theme prompt a schema summary of each input, covering shape, column names, dtypes, null counts, cardinality, and numeric min, max and mean the first sample_rows rows of each input, 3 by default, when data_privacy is &quot;sample&quot; the declared inputs, outputs and actions the full widget code for edits, audits, and runtime repairs runtime error messages, when a repair is running Controlling what leaves the notebook data_privacy chooses between the two summary modes. vw.config(data_privacy=&quot;schema&quot;) # schema only, no cell values vw.config(data_privacy=&quot;sample&quot;) # schema plus head rows, the default vw.config(sample_rows=1) # fewer rows in &quot;sample&quot; mode In &quot;schema&quot; mode the summary carries no cell values at all, so there are no head rows, no category examples and no sampled values. Only the shape and the per-column description are sent. Inputs that are not tables are summarized by type and length, and their truncated repr is included in &quot;sample&quot; mode only. data_privacy governs the summary built from the values you pass to vw.create. It does not restrict the agent&#x27;s data.profile tool, which reads sample values from any file it is allowed to open. Combine data_privacy=&quot;schema&quot; with agent_preset=&quot;safe&quot; to keep the agent out of your project directory as well. No API keys are written to disk. Generated widgets and audit reports are stored locally under .vibewidget/. When a request fails A failed model call raises a ProviderError carrying a short explanation instead of the raw SDK exception. Each error names its kind, which is one of auth, quota, not_found, rate_limit, connection, timeout, context_length or other. A quota error means the key itself is valid but its spending limit is used up, which is worth separating from auth, because rotating a working key does not help. For quota and not_found the provider&#x27;s own sentence is passed through, since it usually names the exact limit or model, and the raw response body is never included."
    },
    {
      "id": "create",
      "path": "/docs/create",
      "title": "Create",
      "description": "Generate a widget from a prompt and a dataset.",
      "content_html": "<p><code>vw.create</code> sends your prompt and a summary of your data to a model, and the model writes the\nJavaScript that draws the widget. The generated code is stored in <code>.vibewidget/</code> in your project,\nso the next call with the same prompt and the same data reuses it instead of paying for a second\nmodel call.</p>\n<div class=\"placeholder\"><div class=\"placeholder-label\">Widget Preview</div><div class=\"placeholder-caption\">Open the live docs to see this interactive widget.</div></div>\n<pre><code class=\"language-python\">import vibe_widget as vw\n\nwidget = vw.create(\n    &quot;bar chart of revenue by region&quot;,\n    df\n)\n\nwidget\n</code></pre>\n<p>The second argument accepts a <code>pandas</code> DataFrame, a path to a data file, a URL, or a directory to\nsearch. Recognized file types include CSV, TSV, JSON, GeoJSON, Parquet, NetCDF, XML, Excel, PDF, and\nplain text. You can leave the argument out when the widget does not read a dataset.</p>\n<h2>When the cache is reused</h2>\n<p>The stored code is found again by a key built from your prompt, the column names and dtypes of the\ndata, the declared inputs and outputs, and the theme description. Changing any one of them produces\na different key, so the model is called again and the earlier widget stays on disk. Row counts are\ndeliberately left out of the key, so appending rows to a DataFrame reuses the widget you already\nhave.\nPass <code>cache=False</code> to force a fresh generation, and use <code>vw.clear()</code> to delete stored widgets.</p>\n<h2>Themes</h2>\n<p>A theme is a description of how the widget should look, written in plain language, and the model\nfollows it while generating the code.</p>\n<pre><code class=\"language-python\">vw.create(&quot;...&quot;, df, theme=&quot;financial_times&quot;)\n</code></pre>\n<p><code>vw.themes()</code> lists the built-in names. To write your own, see <a href=\"/docs/theming\">Theming</a>.</p>\n<h2>Inputs, outputs, and actions</h2>\n<p>Values can flow into a widget from Python, out of a widget back to Python, and from Python as\none-time commands. For the full model and worked examples, see <a href=\"/docs/reactivity\">Reactivity</a>.</p>\n<h2>What the generated code can do</h2>\n<p>Generated JavaScript runs in the notebook page with the same privileges the page has, so it can\nreach the DOM, storage for the notebook origin, and any network address the browser allows. There is\nno browser sandbox around it. Read a generated widget the way you would read a script a stranger\nsent you, and use <code>vw.config(execution=&quot;approve&quot;)</code> when you want to see the code before it runs.</p>",
      "content_text": "vw.create sends your prompt and a summary of your data to a model, and the model writes the JavaScript that draws the widget. The generated code is stored in .vibewidget/ in your project, so the next call with the same prompt and the same data reuses it instead of paying for a second model call. Widget PreviewOpen the live docs to see this interactive widget. import vibe_widget as vw widget = vw.create( &quot;bar chart of revenue by region&quot;, df ) widget The second argument accepts a pandas DataFrame, a path to a data file, a URL, or a directory to search. Recognized file types include CSV, TSV, JSON, GeoJSON, Parquet, NetCDF, XML, Excel, PDF, and plain text. You can leave the argument out when the widget does not read a dataset. When the cache is reused The stored code is found again by a key built from your prompt, the column names and dtypes of the data, the declared inputs and outputs, and the theme description. Changing any one of them produces a different key, so the model is called again and the earlier widget stays on disk. Row counts are deliberately left out of the key, so appending rows to a DataFrame reuses the widget you already have. Pass cache=False to force a fresh generation, and use vw.clear() to delete stored widgets. Themes A theme is a description of how the widget should look, written in plain language, and the model follows it while generating the code. vw.create(&quot;...&quot;, df, theme=&quot;financial_times&quot;) vw.themes() lists the built-in names. To write your own, see Theming. Inputs, outputs, and actions Values can flow into a widget from Python, out of a widget back to Python, and from Python as one-time commands. For the full model and worked examples, see Reactivity. What the generated code can do Generated JavaScript runs in the notebook page with the same privileges the page has, so it can reach the DOM, storage for the notebook origin, and any network address the browser allows. There is no browser sandbox around it. Read a generated widget the way you would read a script a stranger sent you, and use vw.config(execution=&quot;approve&quot;) when you want to see the code before it runs."
    },
    {
      "id": "edit",
      "path": "/docs/edit",
      "title": "Edit",
      "description": "Change a generated widget with a follow-up prompt, in Python or in the widget itself.",
      "content_html": "<p><code>widget.edit(...)</code> describes a change in plain language and returns a new widget with that change\napplied. The existing code goes to the model along with your instruction, so the model revises what\nis there rather than starting over, and the parts you did not mention stay as they were.</p>\n<pre><code class=\"language-python\">chart = vw.create(&quot;scatter plot of price against mileage&quot;, df)\n\nv2 = chart.edit(&quot;add hover tooltips and a right-side legend&quot;)\n</code></pre>\n<p>An edit keeps the theme of the widget it came from, along with its prompt history, so the look stays\nconsistent while you change behavior. Inputs, outputs and actions are not carried over, so declare\nagain any that the new version still needs. To change the look at the same time, pass a different\ntheme:</p>\n<pre><code class=\"language-python\">v3 = v2.edit(&quot;soften the palette and reduce gridlines&quot;, theme=&quot;minimal&quot;)\n</code></pre>\n<p><code>vw.edit(description, source)</code> does the same thing when you would rather name the source\nexplicitly, and the source can be a widget, a handle, or a path to a saved <code>.vw</code> file.</p>\n<h2>Editing one component</h2>\n<p>A generated widget is often made of several pieces, and you can list them and work on one at a time.</p>\n<pre><code class=\"language-python\">widget.components\n# [&#x27;scatter_chart&#x27;, &#x27;color_legend&#x27;, &#x27;slider&#x27;]\n\nwidget.component.color_legend                                  # renders on its own\nwidget.component.color_legend.edit(&quot;use a horizontal layout&quot;)\n</code></pre>\n<p>Each entry in <code>widget.component</code> is a widget in its own right, so it has the same <code>edit</code> and <code>save</code>\nmethods as the whole thing, and it renders when it is the last expression in a cell.</p>\n<h2>Editing inside the widget</h2>\n<p>Every rendered widget has an editor. Opening it shows the generated code, a prompt box for the next\nchange, and the audit findings when an audit has been run. An edit made in the editor updates the\nsame widget in place, and the new code is stored in <code>.vibewidget/</code> the way a generated widget is.</p>\n<p>In approve mode the editor is also where you read the code and approve it, so nothing runs until you\nhave seen it.</p>\n<h2>Reading the history</h2>\n<p>Every prompt that shaped a widget is kept in order, which is useful when a widget has been through\nseveral rounds and you want to know how it got there.</p>\n<pre><code class=\"language-python\">widget.prompt_history\n</code></pre>\n<h2>Cost</h2>\n<p>An edit is a model call, so it is billed like a generation. Reruns with new data are not, because\nthey reuse the code that already exists. See <a href=\"/docs/reactivity\">Reactivity</a> for reruns, and\n<a href=\"/docs/theming\">Theming</a> for keeping a consistent look across edits.</p>",
      "content_text": "widget.edit(...) describes a change in plain language and returns a new widget with that change applied. The existing code goes to the model along with your instruction, so the model revises what is there rather than starting over, and the parts you did not mention stay as they were. chart = vw.create(&quot;scatter plot of price against mileage&quot;, df) v2 = chart.edit(&quot;add hover tooltips and a right-side legend&quot;) An edit keeps the theme of the widget it came from, along with its prompt history, so the look stays consistent while you change behavior. Inputs, outputs and actions are not carried over, so declare again any that the new version still needs. To change the look at the same time, pass a different theme: v3 = v2.edit(&quot;soften the palette and reduce gridlines&quot;, theme=&quot;minimal&quot;) vw.edit(description, source) does the same thing when you would rather name the source explicitly, and the source can be a widget, a handle, or a path to a saved .vw file. Editing one component A generated widget is often made of several pieces, and you can list them and work on one at a time. widget.components # [&#x27;scatter_chart&#x27;, &#x27;color_legend&#x27;, &#x27;slider&#x27;] widget.component.color_legend # renders on its own widget.component.color_legend.edit(&quot;use a horizontal layout&quot;) Each entry in widget.component is a widget in its own right, so it has the same edit and save methods as the whole thing, and it renders when it is the last expression in a cell. Editing inside the widget Every rendered widget has an editor. Opening it shows the generated code, a prompt box for the next change, and the audit findings when an audit has been run. An edit made in the editor updates the same widget in place, and the new code is stored in .vibewidget/ the way a generated widget is. In approve mode the editor is also where you read the code and approve it, so nothing runs until you have seen it. Reading the history Every prompt that shaped a widget is kept in order, which is useful when a widget has been through several rounds and you want to know how it got there. widget.prompt_history Cost An edit is a model call, so it is billed like a generation. Reruns with new data are not, because they reuse the code that already exists. See Reactivity for reruns, and Theming for keeping a consistent look across edits."
    },
    {
      "id": "audit",
      "path": "/docs/audit",
      "title": "Audit",
      "description": "Ask a second model call to review a generated widget and report what it finds.",
      "content_html": "<p>An audit is a second model call that reads the generated code and writes up what it notices. The\nfindings are that model&#x27;s opinion. No static analysis runs behind them, and the package never\nconsults an audit before code executes, so an audit reads like a colleague&#x27;s comments on a pull\nrequest. To gate execution on your own reading of the code, use <code>vw.config(execution=&quot;approve&quot;)</code>.</p>\n<h2>Running an audit</h2>\n<pre><code class=\"language-python\">report = widget.audit(level=&quot;fast&quot;, display=False)\nfull_report = widget.audit(level=&quot;full&quot;, reuse=True, display=False)\n</code></pre>\n<p>A <code>fast</code> audit is a quick scan meant for early iteration, and a <code>full</code> audit covers more ground and\nsuggests alternatives for the choices it flags. <code>reuse=True</code> returns the stored report when the code\nhas not changed since the last audit, instead of paying for another call.</p>\n<p>Audits never run on their own. Nothing is sent to the model until you call <code>widget.audit(...)</code> or\npress the audit button in the widget&#x27;s editor.</p>\n<h2>What the report covers</h2>\n<p>Each finding is filed under one of four areas of the widget.</p>\n<table><thead><tr><th>Area</th><th>What it covers</th></tr></thead><tbody><tr><td>Data</td><td>Selection, transformation, format, provenance</td></tr><tr><td>Computation</td><td>Method, parameters, assumptions, execution</td></tr><tr><td>Presentation</td><td>Encoding, scale, compression, framing</td></tr><tr><td>Interaction</td><td>Triggers, state, propagation, feedback</td></tr></tbody></table>\n<p>A finding is scoped with an id such as <code>data.transformation.date_parsing</code>, and it carries the line\nnumbers in the generated code that it refers to, so you can go straight to the place it describes.</p>\n<h2>The seven lenses</h2>\n<p>In a <code>full</code> audit every finding is also rated along seven fixed lenses, which is what makes one\nfinding comparable to another. A <code>fast</code> audit reports findings without them.</p>\n<ul>\n<li><strong>Impact</strong>, rated high, medium or low, answers whether a different choice would change the conclusions a reader draws.</li>\n<li><strong>Uncertainty</strong> covers confidence, sample size and stability.</li>\n<li><strong>Reproducibility</strong> asks whether the result can be recreated exactly.</li>\n<li><strong>Edge behavior</strong> covers empty, extreme and boundary inputs.</li>\n<li><strong>Default vs explicit</strong> separates a choice you made from an assumption the model made for you.</li>\n<li><strong>Appropriateness</strong> asks whether the method suits the question.</li>\n<li><strong>Safety</strong> covers network calls, dynamic code execution, writes to storage, cross-origin fetches, and injected scripts or iframes.</li>\n</ul>\n<p>The model is told to keep high impact rare and to default to low unless there is clear evidence, so\nmost reports come back full of low-impact findings.</p>\n<h2>Where reports are stored</h2>\n<p>Reports are written to <code>.vibewidget/audits/</code> as JSON and YAML. The directory is listed in\n<code>.vibewidget/.gitignore</code>, so reports stay on the machine that ran them rather than going into your\nrepository.</p>\n<h2>Working from an audit</h2>\n<p>In the widget&#x27;s editor, findings are shown as a checklist next to the code. You can turn any one of\nthem into an edit request, which sends the finding back to the model as the instruction for a fix,\nor leave it on the list as a note for later.</p>",
      "content_text": "An audit is a second model call that reads the generated code and writes up what it notices. The findings are that model&#x27;s opinion. No static analysis runs behind them, and the package never consults an audit before code executes, so an audit reads like a colleague&#x27;s comments on a pull request. To gate execution on your own reading of the code, use vw.config(execution=&quot;approve&quot;). Running an audit report = widget.audit(level=&quot;fast&quot;, display=False) full_report = widget.audit(level=&quot;full&quot;, reuse=True, display=False) A fast audit is a quick scan meant for early iteration, and a full audit covers more ground and suggests alternatives for the choices it flags. reuse=True returns the stored report when the code has not changed since the last audit, instead of paying for another call. Audits never run on their own. Nothing is sent to the model until you call widget.audit(...) or press the audit button in the widget&#x27;s editor. What the report covers Each finding is filed under one of four areas of the widget. AreaWhat it coversDataSelection, transformation, format, provenanceComputationMethod, parameters, assumptions, executionPresentationEncoding, scale, compression, framingInteractionTriggers, state, propagation, feedback A finding is scoped with an id such as data.transformation.date_parsing, and it carries the line numbers in the generated code that it refers to, so you can go straight to the place it describes. The seven lenses In a full audit every finding is also rated along seven fixed lenses, which is what makes one finding comparable to another. A fast audit reports findings without them. Impact, rated high, medium or low, answers whether a different choice would change the conclusions a reader draws. Uncertainty covers confidence, sample size and stability. Reproducibility asks whether the result can be recreated exactly. Edge behavior covers empty, extreme and boundary inputs. Default vs explicit separates a choice you made from an assumption the model made for you. Appropriateness asks whether the method suits the question. Safety covers network calls, dynamic code execution, writes to storage, cross-origin fetches, and injected scripts or iframes. The model is told to keep high impact rare and to default to low unless there is clear evidence, so most reports come back full of low-impact findings. Where reports are stored Reports are written to .vibewidget/audits/ as JSON and YAML. The directory is listed in .vibewidget/.gitignore, so reports stay on the machine that ran them rather than going into your repository. Working from an audit In the widget&#x27;s editor, findings are shown as a checklist next to the code. You can turn any one of them into an edit request, which sends the finding back to the model as the instruction for a fix, or leave it on the list as a note for later."
    },
    {
      "id": "reactivity",
      "path": "/docs/reactivity",
      "title": "Reactivity",
      "description": "Send values into a widget, read values back out, and link widgets together.",
      "content_html": "<p>Values move between Python and a widget in three ways. An input carries a value from Python into the\nwidget and stays set. An output carries a value from the widget back to Python and also stays set. An\naction sends a one-time command from Python to the widget and leaves nothing behind.</p>\n<table><thead><tr><th>Primitive</th><th>Direction</th><th>Persistence</th><th>Use for</th></tr></thead><tbody><tr><td>Input</td><td>Python to JS</td><td>State, sticky</td><td>Data, configuration, selections from other widgets</td></tr><tr><td>Output</td><td>JS to Python</td><td>State, sticky</td><td>User selections, computed values, filters</td></tr><tr><td>Action</td><td>Python to JS</td><td>Event, fires once</td><td>Commands such as reset, focus, export</td></tr></tbody></table>\n<h2>Inputs</h2>\n<p>You declare inputs when you create the widget, either with <code>vw.inputs(...)</code> or with a plain dict. An\ninput keeps its value until you change it.</p>\n<pre><code class=\"language-python\">chart = vw.create(\n    &quot;scatter plot that dims points below the threshold&quot;,\n    df,\n    inputs=vw.inputs(df, threshold=0.5),\n)\n</code></pre>\n<p><code>vw.inputs()</code> takes keyword arguments and bare positional values. A positional value is named after\nthe variable you passed, so <code>vw.inputs(df, threshold=0.5)</code> declares inputs called <code>df</code> and\n<code>threshold</code>.</p>\n<p>Every declared input becomes a synced trait on the widget, so assigning a new value updates the\nwidget without regenerating any code:</p>\n<pre><code class=\"language-python\">chart.threshold = 0.8\n</code></pre>\n<p>Direct assignment sends the value as it is, so it expects something JSON-friendly, meaning numbers,\nstrings, booleans, lists and dicts. NumPy arrays, NumPy scalars and timestamps are converted for you\nwhen the widget is created but not on later assignment, so run them through the same helper:</p>\n<pre><code class=\"language-python\">from vibe_widget.utils.serialization import clean_for_json\n\nchart.frame = clean_for_json(numpy_array)\n</code></pre>\n<p>To swap the underlying dataset, rerun the widget instead of assigning to an input.</p>\n<h2>Rerunning with new data</h2>\n<p><code>vw.create</code> returns a handle, and calling that handle runs the same generated code against different\ndata or inputs, with no second model call:</p>\n<pre><code class=\"language-python\">chart = vw.create(&quot;scatter plot with brush selection&quot;, df)\n\nchart(new_df)                          # same widget, different data\nchart(vw.inputs(threshold=0.7))        # merges with the inputs already declared\nchart(inputs={&quot;threshold&quot;: 0.7})       # replaces the declared inputs\n</code></pre>\n<p>A positional <code>vw.inputs(...)</code> bundle merges into the inputs the widget was created with, while the\n<code>inputs=</code> keyword replaces them. When the new dataset is missing a column the widget was built\nagainst, the rerun raises a <code>ValueError</code> naming the missing columns rather than drawing a broken\nchart.</p>\n<h2>Outputs</h2>\n<p>An output carries a value the widget produced back to Python, and like an input it keeps its value.</p>\n<pre><code class=\"language-python\"># Read the current selection\nprint(widget.outputs.selected_indices.value)\n\n# React to changes\ndef on_selection(change):\n    print(f&quot;Selection changed: {change.new}&quot;)\n\nwidget.outputs.selected_indices.observe(on_selection)\n\n# Stop listening\nwidget.outputs.selected_indices.unobserve(on_selection)\n</code></pre>\n<p>The handler receives the traitlets change object, which carries <code>new</code>, <code>old</code>, <code>name</code> and <code>owner</code>.</p>\n<h2>Actions</h2>\n<p>An action is a one-time command from Python to the widget, for behavior that should happen once\nrather than persist as state.</p>\n<div class=\"placeholder\"><div class=\"placeholder-label\">Widget Preview</div><div class=\"placeholder-caption\">Open the live docs to see this interactive widget.</div></div>\n<pre><code class=\"language-python\">widget = vw.create(\n    &quot;interactive scatter plot&quot;,\n    df,\n    actions=vw.actions(reset_view=&quot;Reset zoom and pan&quot;)\n)\n\nwidget.actions.reset_view()\n</code></pre>\n<p>Once an action fires it is finished, and the widget holds no record that it was reset.</p>\n<h2>Choosing between them</h2>\n<p>Ask whether the value should stick around. When it should, use an input if the value comes from\nPython and an output if it comes from the widget. When it should not, use an action.</p>\n<p>Modelling a one-time command as an input makes it sticky, which is usually wrong:</p>\n<pre><code class=\"language-python\"># Avoid this\nwidget.should_reset = True\n</code></pre>\n<p>The widget would reset and then keep resetting, because the input is still true. An action fires\nonce and leaves nothing set, which avoids the problem.</p>\n<h2>Linking widgets together</h2>\n<p>To connect two widgets, pass one widget&#x27;s output as another widget&#x27;s input.</p>\n<div class=\"placeholder\"><div class=\"placeholder-label\">Widget Preview</div><div class=\"placeholder-caption\">Open the live docs to see this interactive widget.</div></div>\n<pre><code class=\"language-python\">scatter = vw.create(\n    &quot;scatter plot with brush selection tool&quot;,\n    df,\n    outputs=vw.outputs(selected_indices=&quot;indices of selected points&quot;)\n)\n\nhistogram = vw.create(\n    &quot;histogram with highlighted bars for selected data&quot;,\n    vw.inputs(df, selected_indices=scatter.outputs.selected_indices)\n)\n</code></pre>\n<p>Selecting points in the scatter plot updates the histogram through trait syncing. Nothing reruns and\nno model call happens, because the link is a Python observer on the source output that writes\nstraight into the target input.</p>\n<p>A link runs one way, from the named output to the named input. To link in both directions, declare\nan output on each widget and an input on the other.</p>",
      "content_text": "Values move between Python and a widget in three ways. An input carries a value from Python into the widget and stays set. An output carries a value from the widget back to Python and also stays set. An action sends a one-time command from Python to the widget and leaves nothing behind. PrimitiveDirectionPersistenceUse forInputPython to JSState, stickyData, configuration, selections from other widgetsOutputJS to PythonState, stickyUser selections, computed values, filtersActionPython to JSEvent, fires onceCommands such as reset, focus, export Inputs You declare inputs when you create the widget, either with vw.inputs(...) or with a plain dict. An input keeps its value until you change it. chart = vw.create( &quot;scatter plot that dims points below the threshold&quot;, df, inputs=vw.inputs(df, threshold=0.5), ) vw.inputs() takes keyword arguments and bare positional values. A positional value is named after the variable you passed, so vw.inputs(df, threshold=0.5) declares inputs called df and threshold. Every declared input becomes a synced trait on the widget, so assigning a new value updates the widget without regenerating any code: chart.threshold = 0.8 Direct assignment sends the value as it is, so it expects something JSON-friendly, meaning numbers, strings, booleans, lists and dicts. NumPy arrays, NumPy scalars and timestamps are converted for you when the widget is created but not on later assignment, so run them through the same helper: from vibe_widget.utils.serialization import clean_for_json chart.frame = clean_for_json(numpy_array) To swap the underlying dataset, rerun the widget instead of assigning to an input. Rerunning with new data vw.create returns a handle, and calling that handle runs the same generated code against different data or inputs, with no second model call: chart = vw.create(&quot;scatter plot with brush selection&quot;, df) chart(new_df) # same widget, different data chart(vw.inputs(threshold=0.7)) # merges with the inputs already declared chart(inputs={&quot;threshold&quot;: 0.7}) # replaces the declared inputs A positional vw.inputs(...) bundle merges into the inputs the widget was created with, while the inputs= keyword replaces them. When the new dataset is missing a column the widget was built against, the rerun raises a ValueError naming the missing columns rather than drawing a broken chart. Outputs An output carries a value the widget produced back to Python, and like an input it keeps its value. # Read the current selection print(widget.outputs.selected_indices.value) # React to changes def on_selection(change): print(f&quot;Selection changed: {change.new}&quot;) widget.outputs.selected_indices.observe(on_selection) # Stop listening widget.outputs.selected_indices.unobserve(on_selection) The handler receives the traitlets change object, which carries new, old, name and owner. Actions An action is a one-time command from Python to the widget, for behavior that should happen once rather than persist as state. Widget PreviewOpen the live docs to see this interactive widget. widget = vw.create( &quot;interactive scatter plot&quot;, df, actions=vw.actions(reset_view=&quot;Reset zoom and pan&quot;) ) widget.actions.reset_view() Once an action fires it is finished, and the widget holds no record that it was reset. Choosing between them Ask whether the value should stick around. When it should, use an input if the value comes from Python and an output if it comes from the widget. When it should not, use an action. Modelling a one-time command as an input makes it sticky, which is usually wrong: # Avoid this widget.should_reset = True The widget would reset and then keep resetting, because the input is still true. An action fires once and leaves nothing set, which avoids the problem. Linking widgets together To connect two widgets, pass one widget&#x27;s output as another widget&#x27;s input. Widget PreviewOpen the live docs to see this interactive widget. scatter = vw.create( &quot;scatter plot with brush selection tool&quot;, df, outputs=vw.outputs(selected_indices=&quot;indices of selected points&quot;) ) histogram = vw.create( &quot;histogram with highlighted bars for selected data&quot;, vw.inputs(df, selected_indices=scatter.outputs.selected_indices) ) Selecting points in the scatter plot updates the histogram through trait syncing. Nothing reruns and no model call happens, because the link is a Python observer on the source output that writes straight into the target input. A link runs one way, from the named output to the named input. To link in both directions, declare an output on each widget and an input on the other."
    },
    {
      "id": "composability",
      "path": "/docs/composability",
      "title": "Load & Save",
      "description": "Store widgets on disk, share them through git, and reload them later.",
      "content_html": "<p>A widget can be written to a single portable file, and generated code is also kept in your project\nso that a later call reuses it. Both let you come back to a widget without paying for another model\ncall.</p>\n<h2>Save a widget</h2>\n<div class=\"placeholder\"><div class=\"placeholder-label\">Widget Preview</div><div class=\"placeholder-caption\">Open the live docs to see this interactive widget.</div></div>\n<pre><code class=\"language-python\">widget = vw.create(&quot;scatter plot with brush selection&quot;, df)\nwidget.save(&quot;my_widget.vw&quot;)\n</code></pre>\n<p>To store the input values alongside the code, so the widget reloads showing the same state:</p>\n<pre><code class=\"language-python\">widget.save(&quot;my_widget_with_inputs.vw&quot;, include_inputs=True)\n</code></pre>\n<p>A <code>.vw</code> bundle holds the widget code, its metadata, and the signatures of its inputs and outputs.\nWith <code>include_inputs=True</code> the input values are embedded as well.</p>\n<h2>Load a widget</h2>\n<pre><code class=\"language-python\">loaded = vw.load(&quot;my_widget.vw&quot;)\nloaded\n</code></pre>\n<p>Loading uses approval mode by default, so you can read the code before it runs:</p>\n<pre><code class=\"language-python\">loaded = vw.load(&quot;my_widget.vw&quot;, approval=True)\n</code></pre>\n<p>For a file you trust and want to run straight away:</p>\n<pre><code class=\"language-python\">loaded = vw.load(&quot;my_widget.vw&quot;, approval=False)\n</code></pre>\n<p>Calling <code>vw.load()</code> with no path opens a selector listing the widgets cached in your project.</p>\n<h2>Sharing through git</h2>\n<p>Generated code is stored under <code>.vibewidget/</code> in your project, as one <code>.js</code> file and one <code>.json</code>\nsidecar per widget. Commit <code>.vibewidget/widgets/</code> and a collaborator who clones the repository gets\nyour widgets with it. Opening one of them makes no model call, so a collaborator needs no API key\nuntil they generate, edit, or audit something themselves.</p>\n<p>Audit reports are written to <code>.vibewidget/audits/</code>, which is listed in <code>.vibewidget/.gitignore</code>, so\nreports stay on the machine that produced them.</p>\n<h2>Clearing what is stored</h2>\n<p><code>vw.clear()</code> deletes cached widgets and audit reports, forgets the themes generated this session,\nand returns a count of what it removed.</p>\n<pre><code class=\"language-python\">vw.clear()            # everything\nvw.clear(&quot;widgets&quot;)   # generated widget code only\nvw.clear(&quot;audits&quot;)    # audit reports only\nvw.clear(&quot;themes&quot;)    # forget themes generated this session\nvw.clear(widget)      # just that widget&#x27;s code and audits\nvw.clear(&quot;sales_chart&quot;)  # the same, by variable name\n</code></pre>\n<p>Themes you saved with <code>Theme.save</code> are files in <code>.vibewidget/themes/</code> and are not removed by\n<code>vw.clear</code>. Delete the file when you want one gone for good.</p>\n<h2>Before you run a file someone sent you</h2>\n<p>A loaded widget executes JavaScript in your notebook with the privileges of the notebook page. Keep\n<code>approval=True</code> for any file you did not generate yourself, and read the code before approving it.</p>",
      "content_text": "A widget can be written to a single portable file, and generated code is also kept in your project so that a later call reuses it. Both let you come back to a widget without paying for another model call. Save a widget Widget PreviewOpen the live docs to see this interactive widget. widget = vw.create(&quot;scatter plot with brush selection&quot;, df) widget.save(&quot;my_widget.vw&quot;) To store the input values alongside the code, so the widget reloads showing the same state: widget.save(&quot;my_widget_with_inputs.vw&quot;, include_inputs=True) A .vw bundle holds the widget code, its metadata, and the signatures of its inputs and outputs. With include_inputs=True the input values are embedded as well. Load a widget loaded = vw.load(&quot;my_widget.vw&quot;) loaded Loading uses approval mode by default, so you can read the code before it runs: loaded = vw.load(&quot;my_widget.vw&quot;, approval=True) For a file you trust and want to run straight away: loaded = vw.load(&quot;my_widget.vw&quot;, approval=False) Calling vw.load() with no path opens a selector listing the widgets cached in your project. Sharing through git Generated code is stored under .vibewidget/ in your project, as one .js file and one .json sidecar per widget. Commit .vibewidget/widgets/ and a collaborator who clones the repository gets your widgets with it. Opening one of them makes no model call, so a collaborator needs no API key until they generate, edit, or audit something themselves. Audit reports are written to .vibewidget/audits/, which is listed in .vibewidget/.gitignore, so reports stay on the machine that produced them. Clearing what is stored vw.clear() deletes cached widgets and audit reports, forgets the themes generated this session, and returns a count of what it removed. vw.clear() # everything vw.clear(&quot;widgets&quot;) # generated widget code only vw.clear(&quot;audits&quot;) # audit reports only vw.clear(&quot;themes&quot;) # forget themes generated this session vw.clear(widget) # just that widget&#x27;s code and audits vw.clear(&quot;sales_chart&quot;) # the same, by variable name Themes you saved with Theme.save are files in .vibewidget/themes/ and are not removed by vw.clear. Delete the file when you want one gone for good. Before you run a file someone sent you A loaded widget executes JavaScript in your notebook with the privileges of the notebook page. Keep approval=True for any file you did not generate yourself, and read the code before approving it."
    },
    {
      "id": "theming",
      "path": "/docs/theming",
      "title": "Theming",
      "description": "Describe how a widget should look, and reuse that description across widgets.",
      "content_html": "<p>A theme is a description of how a widget should look, written in plain language. The description\ngoes into the prompt, so the colours, type and spacing end up written into the generated JavaScript\nitself. There is no stylesheet layered on afterwards, and changing a theme means generating again.</p>\n<h2>Listing the built-in themes</h2>\n<pre><code class=\"language-python\">import vibe_widget as vw\n\nvw.themes()\n</code></pre>\n<p>In a notebook <code>vw.themes()</code> prints a short list. To get the underlying dict, use <code>dict(vw.themes())</code>.</p>\n<h2>Using a theme</h2>\n<p>Pass the name of a built-in theme:</p>\n<pre><code class=\"language-python\">vw.create(&quot;...&quot;, df, theme=&quot;financial_times&quot;)\n</code></pre>\n<p>A string that is not a registered theme name is treated as a description and sent to the model,\nwhich writes a fresh theme for it. Doing so costs an extra model call and the result varies between\nruns, so use a registered name when you want the same look every time. <code>vw.themes()</code> lists the names\nthat resolve without a model call, and a built-in name works even with no API key set.</p>\n<h2>Writing your own theme</h2>\n<pre><code class=\"language-python\">theme = vw.theme(&quot;like national geographic but greener&quot;)\n\nprint(theme.description)\n\nvw.create(&quot;...&quot;, df, theme=theme)\n</code></pre>\n<p>Pass the <code>Theme</code> object itself rather than <code>theme.description</code>. A description string is not a\nregistered name, so passing it sends you back through generation and produces a different theme.</p>\n<p>To reuse a theme in later sessions, give it a name:</p>\n<pre><code class=\"language-python\">theme.save(&quot;nat_geo_green&quot;)\n\nvw.create(&quot;...&quot;, df, theme=&quot;nat_geo_green&quot;)\n</code></pre>\n<p><code>Theme.save</code> writes a JSON file named after the theme into <code>.vibewidget/themes/</code> in your project and\nregisters the name for the rest of the session. Because the file sits in the project, it can be\ncommitted and shared with the repository. Themes saved by older versions under\n<code>~/.vibewidgets/themes/</code> are still read.</p>\n<h2>Keeping a look across edits</h2>\n<p>An edit reuses the theme of the widget it came from, so the visual language stays the same while you\nchange behavior or layout.</p>\n<div class=\"placeholder\"><div class=\"placeholder-label\">Widget Preview</div><div class=\"placeholder-caption\">Open the live docs to see this interactive widget.</div></div>\n<pre><code class=\"language-python\">v1 = vw.create(&quot;basic scatter&quot;, df, theme=&quot;financial_times&quot;)\n</code></pre>\n<div class=\"placeholder\"><div class=\"placeholder-label\">Widget Preview</div><div class=\"placeholder-caption\">Open the live docs to see this interactive widget.</div></div>\n<pre><code class=\"language-python\">v2 = v1.edit(&quot;add hover tooltips and a right-side legend&quot;)\n</code></pre>\n<p>To change the look on an edit, name a different theme:</p>\n<pre><code class=\"language-python\">v3 = v2.edit(&quot;soften the palette and reduce gridlines&quot;, theme=&quot;minimal&quot;)\n</code></pre>",
      "content_text": "A theme is a description of how a widget should look, written in plain language. The description goes into the prompt, so the colours, type and spacing end up written into the generated JavaScript itself. There is no stylesheet layered on afterwards, and changing a theme means generating again. Listing the built-in themes import vibe_widget as vw vw.themes() In a notebook vw.themes() prints a short list. To get the underlying dict, use dict(vw.themes()). Using a theme Pass the name of a built-in theme: vw.create(&quot;...&quot;, df, theme=&quot;financial_times&quot;) A string that is not a registered theme name is treated as a description and sent to the model, which writes a fresh theme for it. Doing so costs an extra model call and the result varies between runs, so use a registered name when you want the same look every time. vw.themes() lists the names that resolve without a model call, and a built-in name works even with no API key set. Writing your own theme theme = vw.theme(&quot;like national geographic but greener&quot;) print(theme.description) vw.create(&quot;...&quot;, df, theme=theme) Pass the Theme object itself rather than theme.description. A description string is not a registered name, so passing it sends you back through generation and produces a different theme. To reuse a theme in later sessions, give it a name: theme.save(&quot;nat_geo_green&quot;) vw.create(&quot;...&quot;, df, theme=&quot;nat_geo_green&quot;) Theme.save writes a JSON file named after the theme into .vibewidget/themes/ in your project and registers the name for the rest of the session. Because the file sits in the project, it can be committed and shared with the repository. Themes saved by older versions under ~/.vibewidgets/themes/ are still read. Keeping a look across edits An edit reuses the theme of the widget it came from, so the visual language stays the same while you change behavior or layout. Widget PreviewOpen the live docs to see this interactive widget. v1 = vw.create(&quot;basic scatter&quot;, df, theme=&quot;financial_times&quot;) Widget PreviewOpen the live docs to see this interactive widget. v2 = v1.edit(&quot;add hover tooltips and a right-side legend&quot;) To change the look on an edit, name a different theme: v3 = v2.edit(&quot;soften the palette and reduce gridlines&quot;, theme=&quot;minimal&quot;)"
    }
  ]
}