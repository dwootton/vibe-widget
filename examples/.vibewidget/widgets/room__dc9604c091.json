{
  "schema_version": 4,
  "created_at": "2026-09-16T14:29:14.934314+00:00",
  "file_name": "room__dc9604c091.js",
  "var_name": "room",
  "cache_key": "dc9604c091bd8112bb8953152bcaea9d111535c2daaa5866e68b5e02b7b3ff28",
  "description": "2d airflow sandbox for a room, top view, canvas ~600px tall. the room is 8 m wide × 5 m deep. a supply vent (blue, 0.6 m wide) on the left wall and an exhaust (grey, 0.6 m) on the right wall, both draggable along their wall.\n    furniture from data (name, x, y, w, h in metres) as rounded rectangles with their name: drag to move, click a small corner handle to rotate 90°; they are solid obstacles and may not leave the room.\n    run an incompressible 2d fluid solver continuously at ~30 fps on a 96×60 grid: semi-lagrangian advection, a diffusion step, pressure projection with ~24 gauss-seidel iterations, no-slip at walls and furniture, inflow at the vent with speed from a 'fan' slider (0.2–1.5 m/s, start 0.8), outflow at the exhaust. restart the solve smoothly when an obstacle moves (do not reset velocities to zero, just re-mask).\n    draw speed as a viridis heatmap under everything, ~400 tracer particles advected with the flow (respawn at the vent), and hatched 'stale' cells where speed is below a draggable threshold line on a small colour legend (start 0.05 m/s). badge top-left: 'stale air: 23 % of floor'.\n    every time a piece of furniture is dropped, record a snapshot in a strip on the right: a 120px thumbnail of the plan with its stale %, newest first, the lowest % marked with a star; click a thumbnail to restore that layout. keep at most 8.\n    outputs arrangement = list of {name, x, y, w, h} for the current furniture positions, stale_pct = number.",
  "data_signature": {
    "shape": [
      6,
      5
    ],
    "columns": [
      "name",
      "x",
      "y",
      "w",
      "h"
    ],
    "dtypes": [
      [
        "name",
        "str"
      ],
      [
        "x",
        "float64"
      ],
      [
        "y",
        "float64"
      ],
      [
        "w",
        "float64"
      ],
      [
        "h",
        "float64"
      ]
    ]
  },
  "model": "anthropic/claude-opus-5",
  "exports_signature": "cb883e86",
  "imports_signature": "6b543db7",
  "theme_signature": "6e1a5884",
  "parameter_signature": "67e50787ce",
  "prompt_keywords": [
    "airflow",
    "sandbox"
  ],
  "theme_name": "minimal",
  "theme_description": "Minimal instrument, print quality. Nothing on screen that is not data, a control, or a label a reader needs to read the data.\n\n## Text\nNo title, subtitle, heading, description, instructions, hints, tips, how-to text, legend prose, footers, or status sentences. No emoji, check marks, crosses, exclamation marks, capitals for emphasis, or promotional tone. Labels are lowercase or sentence case, terse, and appear only where they carry a value (a count line, a time on a handle, the name of the selected mark). Numbers use tabular figures. If a control needs explaining, redesign the control instead of adding text.\n\n## Type\nOne typeface: system-ui, -apple-system, Inter, Helvetica, sans-serif. 11-13px labels, 12-14px values, weight 400, weight 600 only for the single emphasized value. Monospace (ui-monospace, SF Mono, Menlo) only for times, coordinates and code.\n\n## Color\nPaper #ffffff, ink #111111, grey #777777, hairline #d9d9d9, faint fill #f2f2f2. One accent #d9480f for the current selection or target. Status colours only where they encode a state: open #2b8a3e, closed #c92a2a, unknown grey. Nothing else is coloured. No gradients, shadows, glows, rounded cards, pills, or tinted panels. Buttons and chips: hairline border, no fill; the active one is filled ink with white text.\n\n## Marks\nThin. 1px hairlines, 1.5px data lines, 2px for the selected route. Points 5-7px. Draggable handles: 7px filled ink or accent with a 1.5px white stroke and an invisible 16px hit area. Area fills 8-15% opacity.\n\n## Maps\nLight grey basemap with no API key: Esri World Light Gray Canvas, https://server.arcgisonline.com/ArcGIS/rest/services/Canvas/World_Light_Gray_Base/MapServer/tile/{z}/{y}/{x} (maxZoom 16, attribution 'Esri, HERE, Garmin, OpenStreetMap contributors'), so the data carries the colour. Never use CARTO or Stadia tiles. Small attribution, zoom control bottom-right.\n\n## Layout\nWhite ground, 12px padding, flat. A side panel is separated by a hairline, not boxed. Tables: hairline rows, right-aligned numbers, no zebra striping. Space instead of borders.\n\n## Motion\nNone beyond the direct response to the pointer. State changes apply within 150ms, no easing flourishes.",
  "notebook_path": null,
  "components": [
    "FanSlider",
    "SnapshotStrip",
    "SpeedLegend"
  ],
  "revision_parent": null,
  "prompt_history": [
    {
      "prompt": "2d airflow sandbox for a room, top view, canvas ~600px tall. the room is 8 m wide × 5 m deep. a supply vent (blue, 0.6 m wide) on the left wall and an exhaust (grey, 0.6 m) on the right wall, both draggable along their wall.\n    furniture from data (name, x, y, w, h in metres) as rounded rectangles with their name: drag to move, click a small corner handle to rotate 90°; they are solid obstacles and may not leave the room.\n    run an incompressible 2d fluid solver continuously at ~30 fps on a 96×60 grid: semi-lagrangian advection, a diffusion step, pressure projection with ~24 gauss-seidel iterations, no-slip at walls and furniture, inflow at the vent with speed from a 'fan' slider (0.2–1.5 m/s, start 0.8), outflow at the exhaust. restart the solve smoothly when an obstacle moves (do not reset velocities to zero, just re-mask).\n    draw speed as a viridis heatmap under everything, ~400 tracer particles advected with the flow (respawn at the vent), and hatched 'stale' cells where speed is below a draggable threshold line on a small colour legend (start 0.05 m/s). badge top-left: 'stale air: 23 % of floor'.\n    every time a piece of furniture is dropped, record a snapshot in a strip on the right: a 120px thumbnail of the plan with its stale %, newest first, the lowest % marked with a star; click a thumbnail to restore that layout. keep at most 8.\n    outputs arrangement = list of {name, x, y, w, h} for the current furniture positions, stale_pct = number.",
      "source": "create",
      "timestamp": "2026-09-16T14:23:19.794096+00:00"
    }
  ],
  "outputs": {
    "arrangement": "current furniture rectangles",
    "stale_pct": "percent of floor cells below the stale threshold"
  },
  "inputs": {
    "data": "<input>"
  },
  "actions": {},
  "provenance": {
    "model": "anthropic/claude-opus-5",
    "base_url": "https://openrouter.ai/api/v1",
    "temperature": 0.7,
    "provider_usage": {
      "prompt_tokens": 6420,
      "completion_tokens": 31688,
      "requests": 1
    },
    "vibe_widget_version": "0.3.2"
  }
}